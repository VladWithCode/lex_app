import { HashRouter, Route, Routes } from "react-router";

export default function Router() {
    return (
        <HashRouter>
            <Routes>
            </Routes>
        </HashRouter>
    )
}
